import { RestoreService } from "./restore.service";
export declare class RestoreController {
    private service;
    constructor(service: RestoreService);
    findAll(): never[];
    create(dto: any): any;
}
