"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.AiPublishingPipelineService = void 0;
const common_1 = require("@nestjs/common");
const ai_decision_service_1 = require("../ai-decision/ai-decision.service");
const publish_jobs_service_1 = require("../publish-jobs/publish-jobs.service");
const ai_action_log_service_1 = require("../ai-action-log/ai-action-log.service");
let AiPublishingPipelineService = class AiPublishingPipelineService {
    constructor(decision, publishJobs, actionLog) {
        this.decision = decision;
        this.publishJobs = publishJobs;
        this.actionLog = actionLog;
    }
    async run(vehicleId) {
        const d = await this.decision.evaluateVehicle(vehicleId);
        const channels = this.selectChannels(d);
        const jobs = [];
        for (const channel of channels) {
            const creative = this.buildCreative(vehicleId, channel, d);
            const job = await this.publishJobs.createChannelJob(vehicleId, channel, creative.title, "queued", creative.content, {
                decision: d.overallDecision,
                recommendedPrice: d.recommendedPrice,
                estimatedPrice: d.estimatedPrice,
                creative,
            });
            jobs.push(job);
            await this.actionLog.write(vehicleId, `PUBLISH_JOB_CREATED:${channel}`, "completed");
        }
        return {
            success: true,
            vehicleId,
            decision: d.overallDecision,
            selectedChannels: channels,
            jobsCreated: jobs.length,
            jobs,
        };
    }
    selectChannels(decision) {
        const actions = decision.actions ?? [];
        const marketing = decision.summary?.marketing ?? {};
        const exportOpportunity = decision.summary?.exportOpportunity ?? {};
        const buyer = decision.summary?.buyer ?? {};
        const channels = new Set();
        channels.add("website");
        if (actions.includes("START_MARKETING")) {
            for (const c of marketing.channels ?? [])
                channels.add(c);
        }
        if ((buyer.matchScore ?? 0) >= 85) {
            channels.add("matched_buyers");
            channels.add("crm_leads");
        }
        if ((exportOpportunity.exportScore ?? 0) >= 85) {
            channels.add("gcc_export");
            channels.add("dealer_network");
        }
        return Array.from(channels);
    }
    buildCreative(vehicleId, channel, decision) {
        const valuation = decision.summary?.valuation ?? {};
        const marketing = decision.summary?.marketing ?? {};
        const trust = decision.summary?.trust ?? {};
        const exportOpportunity = decision.summary?.exportOpportunity ?? {};
        const price = decision.recommendedPrice ?? valuation.recommendedPrice ?? null;
        const title = marketing.headline
            ? `${marketing.headline} - ${channel}`
            : `Smart vehicle listing - ${channel}`;
        const content = [
            `Vehicle ID: ${vehicleId}`,
            price ? `Recommended price: ${price}` : null,
            trust.level ? `Trust level: ${trust.level}` : null,
            exportOpportunity.bestCountry
                ? `Best export market: ${exportOpportunity.bestCountry}`
                : null,
            `Generated by AVOS AI Publishing Pipeline`,
        ]
            .filter(Boolean)
            .join(" | ");
        return {
            title,
            content,
            channel,
            price,
            trustLevel: trust.level ?? null,
            exportMarket: exportOpportunity.bestCountry ?? null,
        };
    }
};
exports.AiPublishingPipelineService = AiPublishingPipelineService;
exports.AiPublishingPipelineService = AiPublishingPipelineService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [ai_decision_service_1.AiDecisionService,
        publish_jobs_service_1.PublishJobsService,
        ai_action_log_service_1.AiActionLogService])
], AiPublishingPipelineService);
//# sourceMappingURL=ai-publishing-pipeline.service.js.map