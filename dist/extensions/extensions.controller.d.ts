import { ExtensionsService } from "./extensions.service";
export declare class ExtensionsController {
    private service;
    constructor(service: ExtensionsService);
    findAll(): never[];
    create(dto: any): any;
}
