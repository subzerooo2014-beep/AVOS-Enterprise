import { SummariesService } from "./summaries.service";
export declare class SummariesController {
    private service;
    constructor(service: SummariesService);
    findAll(): never[];
    create(dto: any): any;
}
