import { GeolocationService } from "./geolocation.service";
export declare class GeolocationController {
    private service;
    constructor(service: GeolocationService);
    findAll(): never[];
    create(dto: any): any;
}
