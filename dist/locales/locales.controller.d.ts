import { LocalesService } from "./locales.service";
export declare class LocalesController {
    private service;
    constructor(service: LocalesService);
    findAll(): never[];
    create(dto: any): any;
}
