export declare class PublisherEngineInfoService {
    info(): {
        engine: string;
        version: string;
        mode: string;
        build: string;
        ready: boolean;
        generatedAt: Date;
    };
}
