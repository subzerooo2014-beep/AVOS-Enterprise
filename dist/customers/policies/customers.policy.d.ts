export declare class CustomersPolicy {
    static canRead(user?: any): boolean;
    static canCreate(user?: any): boolean;
    static canWrite(user?: any): boolean;
    static canDelete(user?: any): boolean;
    static canRestore(user?: any): boolean;
    static canExport(user?: any): boolean;
    static canImport(user?: any): boolean;
}
