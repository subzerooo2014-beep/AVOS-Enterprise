"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.AvosKernelService = void 0;
const common_1 = require("@nestjs/common");
const prisma_service_1 = require("../prisma/prisma.service");
let AvosKernelService = class AvosKernelService {
    constructor(prisma) {
        this.prisma = prisma;
    }
    clamp(value) {
        return Math.max(0, Math.min(100, Math.round(value)));
    }
    async decide(data) {
        const actions = [];
        if (data.event === "VehicleCreated" || data.entityType === "vehicle") {
            actions.push("vehicle_valuation", "fraud_assessment", "trust_profile", "buyer_matching", "marketing_campaign", "export_opportunity_check");
        }
        else {
            actions.push("log_only");
        }
        return this.prisma.kernelDecision.create({
            data: {
                eventType: data.event || data.eventType,
                entityType: data.entityType || data.entity,
                entityId: data.entityId,
                decision: actions.includes("log_only") ? "log_only" : "execute_workflow",
                confidence: this.clamp(90),
                reason: "Kernel decision generated by AVOS Kernel.",
                actions,
                status: "approved",
            },
        });
    }
    list() {
        return this.prisma.kernelDecision.findMany({
            orderBy: { createdAt: "desc" },
        });
    }
};
exports.AvosKernelService = AvosKernelService;
exports.AvosKernelService = AvosKernelService = __decorate([
    (0, common_1.Injectable)(),
    __metadata("design:paramtypes", [prisma_service_1.PrismaService])
], AvosKernelService);
//# sourceMappingURL=avos-kernel.service.js.map