import { RealtimeService } from "./realtime.service";
export declare class RealtimeController {
    private service;
    constructor(service: RealtimeService);
    findAll(): never[];
    create(dto: any): any;
}
