import { VectordbService } from "./vectordb.service";
export declare class VectordbController {
    private service;
    constructor(service: VectordbService);
    findAll(): never[];
    create(dto: any): any;
}
