import { Injectable } from "@nestjs/common"; import { SeoPolicy } from "../policies/seo.policy"; @Injectable() export class SeoService { private readonly records:Array<Record<string,unknown>>=[]; constructor(private readonly policy:SeoPolicy){} create(input:any){this.policy.validate(input.path,input.title);const r={id:`seo_${Date.now()}`,...input,status:"PUBLISHED"};this.records.push(r);return r;} list(){return [...this.records];} }
