import { Injectable } from "@nestjs/common"; import { InfluencerPolicy } from "../policies/influencer.policy"; @Injectable() export class InfluencerService { private readonly records:Array<Record<string,unknown>>=[]; constructor(private readonly policy:InfluencerPolicy){} create(input:any){this.policy.validate(input.followers,input.engagementRate);const r={id:`influencer_${Date.now()}`,...input,status:"ACTIVE"};this.records.push(r);return r;} list(){return [...this.records];} }
