import { Injectable } from "@nestjs/common"; @Injectable() export class JourneyGenomeService { private readonly records:Array<Record<string,unknown>>=[]; record(input:any){const r={id:`journey_event_${Date.now()}`,...input,createdAt:new Date().toISOString()};this.records.push(r);return r;} list(){return [...this.records];} }
