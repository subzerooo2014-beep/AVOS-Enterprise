import { Injectable } from "@nestjs/common"; import { RevenuePolicy } from "../policies/revenue.policy"; @Injectable() export class GrowthRevenueService { private readonly records:Array<Record<string,unknown>>=[]; constructor(private readonly policy:RevenuePolicy){} record(input:any){this.policy.validate(input.amount,input.cost);const r={id:`growth_revenue_${Date.now()}`,...input,profit:input.amount-input.cost};this.records.push(r);return r;} list(){return [...this.records];} }
