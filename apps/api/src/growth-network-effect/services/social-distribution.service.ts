import { Injectable } from "@nestjs/common"; import { SocialPolicy } from "../policies/social.policy"; @Injectable() export class SocialDistributionService { private readonly records:Array<Record<string,unknown>>=[]; constructor(private readonly policy:SocialPolicy){} publish(input:any){this.policy.validate(input.channel,input.content);const r={id:`social_${Date.now()}`,...input,status:input.scheduledAt?"SCHEDULED":"PUBLISHED"};this.records.push(r);return r;} list(){return [...this.records];} }
