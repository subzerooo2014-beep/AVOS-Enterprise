import { Injectable } from "@nestjs/common"; import { AbTestPolicy } from "../policies/ab-test.policy"; @Injectable() export class AbTestService { private readonly records:Array<Record<string,unknown>>=[]; constructor(private readonly policy:AbTestPolicy){} create(input:any){const split=input.splitPercent??50;this.policy.validate(split);const r={id:`ab_${Date.now()}`,...input,splitPercent:split,status:"RUNNING"};this.records.push(r);return r;} list(){return [...this.records];} }
