import { Injectable } from "@nestjs/common"; import { RetentionPolicy } from "../policies/retention.policy"; @Injectable() export class RetentionService { private readonly records:Array<Record<string,unknown>>=[]; constructor(private readonly policy:RetentionPolicy){} record(input:any){this.policy.validate(input.activeDays,input.totalDays);const r={id:`retention_${Date.now()}`,...input,rate:Math.round((input.activeDays/input.totalDays)*100)};this.records.push(r);return r;} list(){return [...this.records];} }
