import { Injectable } from "@nestjs/common"; @Injectable() export class SeoIntelligenceEngine { analyze(input:{keywords:number;contentLength:number;backlinks:number;technicalScore:number}){ const score=Math.min(100,Math.round(input.keywords*2+input.contentLength/100+input.backlinks*.5+input.technicalScore*.4)); return {score,status:score>=80?"STRONG":score>=60?"GOOD":"NEEDS_WORK"}; } }
