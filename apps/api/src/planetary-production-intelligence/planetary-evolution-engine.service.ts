import { Injectable,NotFoundException } from "@nestjs/common";import { PlanetaryProductionIntelligenceStore as S } from "./planetary-production-intelligence.store";@Injectable() export class PlanetaryEvolutionEngineService{constructor(private readonly s:S){}propose(){const x:any={id:this.s.id("evolution"),title:"Sovereign Federation Adaptive Expansion",rationale:"Improve resilience and jurisdiction coverage",expectedImpact:94,riskLevel:"medium",status:"proposed",createdAt:this.s.now()};this.s.evolutions.push(x);return x}approve(id:string,by="human:khalifa"){const x=this.s.evolutions.find(e=>e.id===id);if(!x)throw new NotFoundException(id);x.status="approved";x.approvedBy=by;return x}deploy(id:string){const x=this.s.evolutions.find(e=>e.id===id);if(!x)throw new NotFoundException(id);if(x.status!=="approved")throw new Error("Human approval required");x.status="deployed";return x}}