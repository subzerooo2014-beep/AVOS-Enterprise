import { Injectable,NotFoundException } from "@nestjs/common";import { PlanetaryProductionIntelligenceStore as S } from "./planetary-production-intelligence.store";import { PlanetaryPolicyEngineService as P } from "./planetary-policy-engine.service";
@Injectable() export class GlobalWorkloadFederationService{constructor(private readonly s:S,private readonly p:P){}create(i:any){const n=this.s.now();const w:any={id:this.s.id("workload"),objective:i.objective??"Planetary sovereign production",requiredCapabilities:i.requiredCapabilities??["software-factory"],capacity:i.capacity??50,classification:i.classification??"confidential",origin:i.origin??"AE",preferredRegions:i.preferredRegions??["middle-east","europe"],prohibited:i.prohibited??[],requiresHumanApproval:i.requiresHumanApproval??true,humanDecision:"pending",status:"created",createdAt:n,updatedAt:n};this.s.workloads.push(w);return w}federate(id:string){const w:any=this.s.workloads.find(x=>x.id===id);if(!w)throw new NotFoundException(id);const c=this.s.grids.map(g=>{const e=this.p.evaluate(w,g);const score=e.decision==="denied"?0:Number((g.trust*.2+g.sovereignty*.2+g.compliance*.2+(100-g.cost)*.1+(100-g.carbon)*.1+(100-g.latency)*.1+Math.min(100,g.capacity/10)*.1+(w.preferredRegions.includes(g.region)?8:0)).toFixed(2));return{gridId:g.id,totalScore:score,decision:e.decision}}).sort((a,b)=>b.totalScore-a.totalScore);w.selectedGridIds=c.filter(x=>x.decision!=="denied"&&x.totalScore>0).slice(0,2).map(x=>x.gridId);w.status="federated";w.updatedAt=this.s.now();const z={id:this.s.id("coordination"),workloadId:id,selectedGridIds:w.selectedGridIds,candidateScores:c,status:w.selectedGridIds.length?"selected":"blocked",requiresHumanApproval:w.requiresHumanApproval,createdAt:this.s.now()};this.s.coordinations.push(z);return z}approve(id:string,by="human:khalifa"){const w:any=this.s.workloads.find(x=>x.id===id);if(!w)throw new NotFoundException(id);w.humanDecision="approved";w.status="approved";w.updatedAt=this.s.now();return{...w,approvedBy:by}}}