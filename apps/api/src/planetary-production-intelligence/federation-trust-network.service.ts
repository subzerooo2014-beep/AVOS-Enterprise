import { Injectable } from "@nestjs/common";import { PlanetaryProductionIntelligenceStore as S } from "./planetary-production-intelligence.store";@Injectable() export class FederationTrustNetworkService{constructor(private readonly s:S){}establish(a:string,b:string){const x=this.s.grids.find(g=>g.id===a),y=this.s.grids.find(g=>g.id===b);const score=x&&y?Number(((x.trust+y.trust+x.compliance+y.compliance)/4).toFixed(2)):0;const z={id:this.s.id("trust"),sourceGridId:a,targetGridId:b,trustScore:score,status:score>=90?"trusted":"review-required",createdAt:this.s.now()};this.s.trustLinks.push(z);return z}}