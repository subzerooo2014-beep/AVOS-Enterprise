import { Injectable } from "@nestjs/common";import { PlanetaryProductionIntelligenceStore as S } from "./planetary-production-intelligence.store";@Injectable() export class PlanetaryDigitalTwinService{constructor(private readonly s:S){}snapshot(){const a=(v:number[])=>Number((v.reduce((x,y)=>x+y,0)/v.length).toFixed(2));return{id:this.s.id("twin"),sovereignGrids:this.s.grids.length,operationalGrids:this.s.grids.filter(g=>g.status==="operational").length,workloads:this.s.workloads.length,policyEvaluations:this.s.policies.length,knowledgeExchanges:this.s.exchanges.length,trustLinks:this.s.trustLinks.length,globalTrust:a(this.s.grids.map(g=>g.trust)),globalSovereignty:a(this.s.grids.map(g=>g.sovereignty)),globalCompliance:a(this.s.grids.map(g=>g.compliance)),capacity:this.s.grids.reduce((x,g)=>x+g.capacity,0),createdAt:this.s.now()}}}