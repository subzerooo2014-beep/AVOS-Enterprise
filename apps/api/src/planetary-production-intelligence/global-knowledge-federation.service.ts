import { Injectable } from "@nestjs/common";import { PlanetaryProductionIntelligenceStore as S } from "./planetary-production-intelligence.store";@Injectable() export class GlobalKnowledgeFederationService{constructor(private readonly s:S){}exchange(i:any){const x={id:this.s.id("knowledge"),sourceGridId:i.sourceGridId,targetGridIds:i.targetGridIds,domain:i.domain,mode:i.mode??"federated-learning",privacyPreserved:true,status:"approved",createdAt:this.s.now()};this.s.exchanges.push(x);return x}}