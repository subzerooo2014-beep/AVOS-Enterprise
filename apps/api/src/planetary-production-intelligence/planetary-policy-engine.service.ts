import { Injectable } from "@nestjs/common";import { PlanetaryProductionIntelligenceStore as S } from "./planetary-production-intelligence.store";
@Injectable() export class PlanetaryPolicyEngineService{constructor(private readonly s:S){}evaluate(w:any,g:any){let decision:"allowed"|"restricted"|"denied"="allowed";const reasons:string[]=[];let score=100;if(w.prohibited.some((x:string)=>g.jurisdictions.includes(x))){decision="denied";score=0;reasons.push("prohibited jurisdiction")}if(!w.requiredCapabilities.every((x:string)=>g.capabilities.includes(x))){decision="denied";score=0;reasons.push("missing capability")}if(g.dataResidency&&!g.jurisdictions.includes(w.origin)&&decision!=="denied"){decision="restricted";score-=15;reasons.push("federated execution only")}if(!reasons.length)reasons.push("all policies passed");const e={id:this.s.id("policy"),workloadId:w.id,gridId:g.id,decision,reasons,score,createdAt:this.s.now()};this.s.policies.push(e);return e}}