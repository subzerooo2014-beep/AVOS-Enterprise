import { Injectable } from "@nestjs/common"; import { RuntimeTenantPolicy } from "../policies/tenant.policy"; @Injectable() export class RuntimeTenantService { private readonly records:Array<Record<string,unknown>>=[]; constructor(private readonly policy:RuntimeTenantPolicy){} create(input:any){this.policy.validate(input.tenantId,input.name);const r={...input,enabled:true,createdAt:new Date().toISOString()};this.records.push(r);return r;} list(){return [...this.records];} }
