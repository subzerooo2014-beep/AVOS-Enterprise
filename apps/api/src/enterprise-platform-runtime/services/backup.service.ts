import { Injectable } from "@nestjs/common"; import { BackupPolicy } from "../policies/backup.policy"; @Injectable() export class BackupService { private readonly records:Array<Record<string,unknown>>=[]; constructor(private readonly policy:BackupPolicy){} create(input:any){this.policy.validate(input.scope,input.destination);const r={id:`backup_${Date.now()}`,...input,status:"COMPLETED"};this.records.push(r);return r;} list(){return [...this.records];} }
