import { Injectable } from "@nestjs/common"; import { RuntimeWorkflowPolicy } from "../policies/workflow.policy"; @Injectable() export class RuntimeWorkflowService { private readonly records:Array<Record<string,unknown>>=[]; constructor(private readonly policy:RuntimeWorkflowPolicy){} create(input:any){this.policy.validate(input.name,input.steps);const r={id:`workflow_${Date.now()}`,...input,status:"ACTIVE"};this.records.push(r);return r;} list(){return [...this.records];} }
