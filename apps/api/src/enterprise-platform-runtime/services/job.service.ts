import { Injectable } from "@nestjs/common"; import { RuntimeJobPolicy } from "../policies/job.policy"; @Injectable() export class RuntimeJobService { private readonly records:Array<Record<string,unknown>>=[]; constructor(private readonly policy:RuntimeJobPolicy){} create(input:any){const priority=input.priority??50;this.policy.validate(input.type,priority);const r={id:`job_${Date.now()}`,...input,priority,status:"QUEUED",attempts:0,createdAt:new Date().toISOString()};this.records.push(r);return r;} list(){return [...this.records];} }
