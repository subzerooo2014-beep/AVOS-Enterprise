import { Injectable } from "@nestjs/common"; @Injectable() export class InsurancePricingEngine { calculate(input:{vehicleValue:number;driverRisk:number;coverageFactor:number;claims:number}){ const premium=input.vehicleValue*.025*input.coverageFactor*(1+input.driverRisk/200+input.claims*.1); return {premium:Math.round(premium),riskBand:input.driverRisk>=70?"HIGH":input.driverRisk>=40?"MEDIUM":"LOW"}; } }
