import { Injectable } from "@nestjs/common"; import { InsurancePolicy } from "../policies/insurance.policy"; @Injectable() export class InsuranceQuoteService { private readonly records:Array<Record<string,unknown>>=[]; constructor(private readonly policy:InsurancePolicy){} create(input:any){this.policy.validate(input.declaredValue,input.coverageType);const r={id:`insurance_quote_${Date.now()}`,...input,status:"QUOTED"};this.records.push(r);return r;} list(){return [...this.records];} }
