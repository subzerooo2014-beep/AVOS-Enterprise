import { Injectable } from "@nestjs/common"; import { SettlementPolicy } from "../policies/settlement.policy"; @Injectable() export class FinancialSettlementService { private readonly records:Array<Record<string,unknown>>=[]; constructor(private readonly policy:SettlementPolicy){} create(input:any){this.policy.validate(input.grossAmount,input.fees);const r={id:`settlement_${Date.now()}`,...input,netAmount:input.grossAmount-input.fees,status:"COMPLETED"};this.records.push(r);return r;} list(){return [...this.records];} }
