import { Injectable } from "@nestjs/common"; import { ClaimPolicy } from "../policies/claim.policy"; @Injectable() export class InsuranceClaimService { private readonly records:Array<Record<string,unknown>>=[]; constructor(private readonly policy:ClaimPolicy){} create(input:any){this.policy.validate(input.description,input.estimatedAmount);const r={id:`insurance_claim_${Date.now()}`,...input,status:"SUBMITTED"};this.records.push(r);return r;} list(){return [...this.records];} }
