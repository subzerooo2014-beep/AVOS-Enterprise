import { Injectable } from "@nestjs/common"; @Injectable() export class ServiceRecommendationEngine { recommend(input:{healthScore:number;mileageSinceService:number;recallOpen:boolean}){ const actions:string[]=[]; if(input.healthScore<60) actions.push("diagnostic"); if(input.mileageSinceService>=10000) actions.push("scheduled_service"); if(input.recallOpen) actions.push("recall_action"); return {actions:actions.length?actions:["monitor"]}; } }
