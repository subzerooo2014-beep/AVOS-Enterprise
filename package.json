{
  "name": "avos",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "api": "pnpm --dir apps/api dev",
    "web": "pnpm --dir apps/web dev"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
