global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: avos-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - avos-api:3000

rule_files:
  - alerts.yml